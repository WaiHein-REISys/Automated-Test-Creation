# AGENTS.md

All agent instructions are centralized in [agent.md](./agent.md). Read that file first.

You are the primary interface — the user should not need to run commands manually. Execute the ATC pipeline, edit `cli/run.json`, and generate feature files on their behalf. See `agent.md` for the full zero-touch workflow.
